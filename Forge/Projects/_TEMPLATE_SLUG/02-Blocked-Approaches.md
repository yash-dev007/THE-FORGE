# Blocked Approaches — _TEMPLATE_SLUG

Replace `_TEMPLATE_SLUG` with your `ForgeProjectSlug` (from `FORGE_IDENTITY.md`).

This file mirrors the `Blocked Approaches` section of `RESEARCH.md` but in Obsidian
for cross-project reference. Update it manually after each REVERT decision.

## Format

```
### Cycle N — YYYY-MM-DD
**Hypothesis:** <what was tried>
**Why it failed:** <mechanism that did not work as expected>
**Do not retry:** <what specifically to avoid>
```

---

### Cycle 0 — (template)
**Hypothesis:** (fill in after your first REVERT)
**Why it failed:** (mechanism explanation)
**Do not retry:** (specific pattern to avoid in future hypotheses)
