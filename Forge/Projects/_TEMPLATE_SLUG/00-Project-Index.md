# Forge project index — `_TEMPLATE_SLUG`

Replace `_TEMPLATE_SLUG` with the **`ForgeProjectSlug`** from the adopted repository’s `FORGE_IDENTITY.md` (must be unique in this vault).

## Links

- Git remote (optional):
- Local clone path:
- This repo’s `PROJECT_LOG.md` path (outside Obsidian):

## Session notes

Scratchpad for **this project only**. Do not store other projects’ hypotheses here.

## Promotion queue

Patterns observed here but not yet global — move to `Forge/Patterns/` when confirmed on **2+ projects** per v3.
