# Forge / Patterns (global)

Cross-project patterns promoted per THE FORGE v3 rules live here.

- **Read scope:** any session may read these for pattern matching.
- **Write scope:** humans promote from `PROJECT_LOG.md` insights; update `confirmation_count` when a pattern is reinforced.

Use the schema from `templates/universal/CLAUDE.md` (Pattern note schema). See `_example-performance-pattern.md` for a starter note.
